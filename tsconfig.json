{
  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions": {
    "types": ["nuxt", "node"],
    "allowJs": true,
    "strict": true
  },
  "include": [
    "types/**/*.d.ts", // ← pour tes types globaux
    "env.d.ts",         // ← si tu as un fichier d'env typé
    "**/*.ts",
    "**/*.vue"
  ]
}
